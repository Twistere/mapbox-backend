version: '3.9'

services:
  adminer:
    image: adminer
    restart: always
    ports:
      - 9091:8080
    links:
      - postgres-db

  postgres-db:
    image: postgres:14
    restart: always
    ports:
      - 5432:5432
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: ${PWD}
      POSTGRES_DB: ONF
